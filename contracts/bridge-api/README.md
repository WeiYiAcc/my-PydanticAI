# bridge API

This folder describes the shape expected from worker bridges.

Bridges are transport adapters that expose a backend worker over a normalized interface.

## Current bridge examples

- `bridges/pi-mcp-serve.js`
- `bridges/hermes-task-mcp-serve.js`

## Future bridge candidates

- Claude Code task bridge
- OpenAgent task bridge
- OpenClaw remote task bridge
