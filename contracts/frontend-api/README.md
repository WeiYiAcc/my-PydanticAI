# frontend API

This folder documents the host-facing entrypoints into `my-PydanticAI`.

## Current frontends

- CLI (`cli.py` / `my-pydanticai`)
- Telegram bot (`telegram_bot.py`)

## Future frontends / hosts

- Pi invoking orchestrator tasks
- Hermes invoking orchestrator tasks
- OpenClaw remote orchestration
- other TUI or local control-plane wrappers
